[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lerobot_camera_zed2i"
version = "0.1.0"
description = "LeRobot camera plugin for Stereolabs ZED 2i via subprocess bridge"
requires-python = ">=3.10"
