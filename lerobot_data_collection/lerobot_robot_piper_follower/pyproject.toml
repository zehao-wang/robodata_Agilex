[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lerobot_robot_piper_follower"
version = "0.1.0"
description = "LeRobot robot plugin for AgileX PIPER slave arm (master-slave teleoperation)"
requires-python = ">=3.10"
