[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "lerobot_teleoperator_piper_leader"
version = "0.1.0"
description = "LeRobot teleoperator plugin for AgileX PIPER master arm (master-slave teleoperation)"
requires-python = ">=3.10"
